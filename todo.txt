do not just apply some common regex
have a list of common regex and apply them one by one
always ask user to confirm the action and keep a copy.

do not write a generic app for now.
fix my own problem first but keep a list of regexes for later to put in the app

have a test run method that does not change and only shows. for this method have a flag to show non matching or not.
also show before and after.

--
 

for commandline
	http://stackoverflow.com/questions/7691599/execute-python-script-with-function-from-command-line-linux

do not write a generic music manager now. just fix your own music first!
add a pprint to list meta by a regex
